/goal Выполни docs/tasks/GOAL-001-FIRST-DEVICE/TASK.md в C:\Users\ZBook\Mostik\.

Сначала прочитай AGENTS.md, README.md, docs/VALIDATION.md и BUILD_WINDOWS.md.
Репозиторий проекта: https://github.com/kpCat/Mostik.git
Целевая ветка: master.
Для этой задачи Git commit и git push ЯВНО РАЗРЕШЕНЫ И ОБЯЗАТЕЛЬНЫ; это отменяет
старое ограничение «Git push не выполнять», если оно встретится в старом контексте.

1. Проверь git status/branch/remote. Если origin отсутствует — добавь
   https://github.com/kpCat/Mostik.git. Если origin указывает не туда — приведи его
   к указанному репозиторию, не используя force и не теряя пользовательские изменения.
2. Подготовь окружение Visual Studio 2026/.NET for Android: проверь установленные
   компоненты, установи только необходимые недостающие зависимости, не повреждая
   другие проекты и существующие SDK/JDK.
3. Доведи исходники до реальной успешной сборки, исправь ошибки интеграции и собери
   устанавливаемый APK для Redmi Note 9 Pro в
   artifacts/Mostik-0.1.0-debug.apk.
4. Сохрани два направления RU/PL, реальное локальное распознавание Vosk,
   локальный перевод ML Kit и офлайн-озвучку. Не заменяй компоненты заглушками,
   платными или облачными API.
5. Выполни Core.Smoke. При наличии разрешённого ADB-устройства установи APK и
   проверь доступные аппаратные сценарии. При отсутствии телефона выпусти APK
   и честно отметь аппаратные проверки NOT_TESTED; не жди устройство бесконечно.
6. Обязателен docs/tasks/GOAL-001-FIRST-DEVICE/RESULT.md: изменения, команды,
   exit codes, версии SDK/JDK, выполненные проверки, NOT_TESTED/FAIL, путь APK,
   SHA-256 APK, известные риски, локальный commit SHA и результат push.
7. Перед commit проверь staged-файлы. Не коммить секреты/токены, .tools,
   Android SDK/JDK, скачанные модели, bin/obj, APK/AAB, keystore, log/binlog
   или персональные данные.
8. Закоммить исходники, документацию, lockfiles и RESULT.md понятным коммитом.
   Для первой публикации можно использовать:
   Initial Mostik RU-PL offline translator
9. Выполни git push -u origin master. Не используй force-push.
10. После push проверь, что удалённый refs/heads/master имеет тот же SHA, что HEAD.
    Если push заблокирован авторизацией/политикой GitHub, не обходи защиту:
    оставь локальный commit и запиши точную ошибку в RESULT.md.
